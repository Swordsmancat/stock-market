# Implementation

1. Add provider fixture tests for pagination, timestamps, numeric nulls,
   identity fallback, and schema/provider failures.
2. Add ORM model plus Alembic `0025` migration and service upsert/query tests.
3. Add thin API GET/POST routes with validation and sanitized errors.
4. Add Next.js same-origin refresh proxy, backend GET fetch, localized panel,
   and component/page/proxy tests.
5. Update runbooks and executable Trellis contract.
6. Run focused and full backend/Web/type/JSON/migration checks.
7. Deploy API/Web, migrate the database, perform one current-month refresh,
   browser-smoke both locales, commit, archive, journal, and push.
