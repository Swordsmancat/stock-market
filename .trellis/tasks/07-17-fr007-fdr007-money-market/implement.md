# Implementation

1. Add failing provider tests for mappings, order, missing values, and the
   two-month default fetch window.
2. Extend the central AkShare family/source registry and indicator definitions.
3. Add dashboard grouping and localized built-in labels.
4. Update counts and focused service/API/frontend regressions.
5. Update source registry and refresh runbook.
6. Run Trellis checks, deploy the Web/API-compatible change, perform a live
   explicit repo-family refresh, verify ports 3000/8000, commit, push, archive.
