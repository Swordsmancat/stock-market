# Daily acceptance evidence

Each sampled trusted watermark has one sanitized `YYYY-MM-DD.json` observation
file. The matching file in `../reservations/` is the authoritative at-most-once
marker; this directory is a reporting projection.
