# Reservation markers

One `YYYY-MM-DD.json` file is created with operating-system exclusive-create
semantics before any assistant request for that trusted watermark. Existing
markers are never overwritten or retried. Only JSON marker files count toward
the sample.
